<div class="well">
    <h1>A deeper look into the origins of Pilates</h1>
    <h4>This Q&A site is dedicated to questions about the history of Pilates</h4>
</div>

<div class="container">
  <% @questions.each do |q| %>
    <div class="media">
      <div class="media-left">
        <a href="#">
          <img class="media-object" src="<%= q.gravatar %>">
        </a>
      </div>
      <div class="media-body">
        <h4 class="media-heading"><%= q.email %> asked:</h4>
        <div><%= time_ago_in_words q.created_at %> ago</div>
        <div><%= q.body %></div>
        <div><a href="<%= question_path(q) %>" class="btn btn-success btn-xs">View Answers</a></div>
      </div>
    </div>
  <% end %>
</div>
